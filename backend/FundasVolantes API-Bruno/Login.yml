info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/login"
  body:
    type: json
    data: |-
      {
          "email": "admin@fundasvolantes.com",
          "password": "Admin#123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
