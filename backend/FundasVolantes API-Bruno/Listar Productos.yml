info:
  name: Listar Productos
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/productos"
  headers:
    - name: Authorization
      value: Bearer {{token}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
