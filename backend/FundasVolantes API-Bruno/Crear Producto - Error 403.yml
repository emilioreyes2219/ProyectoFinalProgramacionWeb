info:
  name: Crear Producto - Error 403
  type: http
  seq: 5

http:
  method: POST
  url: "{{base_url}}/productos"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
