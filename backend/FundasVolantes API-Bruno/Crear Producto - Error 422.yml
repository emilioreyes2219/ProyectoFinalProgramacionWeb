info:
  name: Crear Producto - Error 422
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/productos"
  headers:
    - name: Authorization
      value: Bearer {{token}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "nombre": "Producto incompleto"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
